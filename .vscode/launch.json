{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Run SFML App",
            "type": "cppvsdbg",
            "request": "launch",
            "program": "${workspaceFolder}/PlanetSim.exe",
            "cwd": "${workspaceFolder}",
            "console": "integratedTerminal"
        }
    ]
}